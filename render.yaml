services:
  - type: worker  # ¡CRÍTICO! Usa 'worker' no 'web' para bots
    name: telegram-uploader-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
    autoDeploy: true
